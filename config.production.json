{
  "_comment": "Ghost Production Configuration / Ghost Production Konfigürasyonu",
  "_warning": "Update domain and email settings before production use / Production kullanımından önce domain ve e-posta ayarlarını güncelleyin",
  "url": "https://your-domain.com",
  "server": {
    "port": 2368,
    "host": "0.0.0.0"
  },
  "database": {
    "_comment": "MySQL database configuration / MySQL veritabanı konfigürasyonu",
    "client": "mysql",
    "connection": {
      "host": "mysql",
      "user": "ghost",
      "password": "your-db-password",
      "database": "ghost_production"
    }
  },
  "mail": {
    "_comment": "SMTP email configuration / SMTP e-posta konfigürasyonu",
    "transport": "SMTP",
    "options": {
      "service": "Gmail",
      "auth": {
        "user": "your-email@gmail.com",
        "pass": "your-app-password"
      }
    },
    "from": "your-email@gmail.com"
  },
  "logging": {
    "_comment": "Production logging settings / Production log ayarları",
    "transports": [
      "file",
      "stdout"
    ],
    "level": "info"
  },
  "process": "systemd",
  "paths": {
    "contentPath": "/var/lib/ghost/content"
  },
  "privacy": {
    "_comment": "Disable update checks for privacy / Gizlilik için güncelleme kontrollerini devre dışı bırak",
    "useUpdateCheck": false
  }
}
