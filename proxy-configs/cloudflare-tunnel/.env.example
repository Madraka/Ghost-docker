# Environment Variables for Cloudflare Tunnel Setup
# Cloudflare Tunnel Kurulumu için Ortam Değişkenleri

# Database Configuration / Veritabanı Konfigürasyonu
DB_ROOT_PASSWORD=your-strong-root-password
DB_PASSWORD=your-strong-ghost-password

# Ghost Configuration / Ghost Konfigürasyonu
GHOST_VERSION=alpine
GHOST_URL=https://your-domain.com

# Cloudflare Tunnel Configuration / Cloudflare Tunnel Konfigürasyonu
# Get this token from Cloudflare Zero Trust Dashboard
# Bu token'ı Cloudflare Zero Trust Dashboard'undan alın
CLOUDFLARE_TUNNEL_TOKEN=your-cloudflare-tunnel-token

# Mail Configuration / E-posta Konfigürasyonu
MAIL_SERVICE=Gmail
MAIL_USER=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_FROM=your-email@gmail.com
