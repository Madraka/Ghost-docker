# Nginx Proxy Configuration / Nginx Proxy Konfigürasyonu
# Copy this file to .env and edit the values
# Bu dosyayı .env olarak kopyalayın ve değerleri düzenleyin

# Database Configuration / Veritabanı Konfigürasyonu
DB_ROOT_PASSWORD=your_strong_root_password_here
DB_PASSWORD=your_strong_ghost_password_here

# Ghost Configuration / Ghost Konfigürasyonu
GHOST_URL=https://yourdomain.com
# Change to your actual domain / Gerçek domain'inizi yazın

# Mail Configuration (Optional) / Mail Konfigürasyonu (Opsiyonel)
# Uncomment and configure for email functionality
# Email işlevselliği için yorumu kaldırın ve yapılandırın
# MAIL_TRANSPORT=SMTP
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_FROM=your-email@gmail.com

# Domain for SSL / SSL için domain
DOMAIN=yourdomain.com
